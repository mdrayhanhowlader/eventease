
import { Role, Page } from './types';

export const ROLES: Role[] = [Role.USER, Role.ORGANIZER, Role.ADMIN];
export const PAGES: Page[] = [Page.HOME, Page.USER_DASHBOARD, Page.ORGANIZER_DASHBOARD, Page.ADMIN_DASHBOARD];
   